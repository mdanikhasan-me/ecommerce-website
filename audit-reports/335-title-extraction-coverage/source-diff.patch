diff --git a/tests/boilabin-advisor-workflow.test.ts b/tests/boilabin-advisor-workflow.test.ts
index ae58a0c..fb47049 100644
--- a/tests/boilabin-advisor-workflow.test.ts
+++ b/tests/boilabin-advisor-workflow.test.ts
@@ -9,6 +9,7 @@ import {
   createBoilabinAdvisorState,
   extractLatestCommit,
   extractSectionSummary,
+  extractTitle,
   findRecommendedBroadStaging,
   findSuspiciousSecrets,
   formatBoilabinAdvisorState,
@@ -254,6 +255,11 @@ test('Advisor latest commit extraction ignores invalid commit references', () =>
   assert.equal(extractLatestCommit('Commit hash: `zzzzzzz not hex`'), null);
 });

+test('Advisor title extraction reads the first markdown H1 only', () => {
+  assert.equal(extractTitle(['Intro', '', '# Step 999 Report Title', '', '## Scope'].join('\n')), 'Step 999 Report Title');
+  assert.equal(extractTitle('## Scope\nNo top-level heading'), null);
+});
+
 test('Advisor section summaries stop cleanly instead of cutting mid-line', () => {
   const oversizedBullet = `- ${'validated-helper-output '.repeat(40)}`;
   const content = [
diff --git a/tests/boilabin-terminal-loop-workflow.test.ts b/tests/boilabin-terminal-loop-workflow.test.ts
index 535ee69..59479ba 100644
--- a/tests/boilabin-terminal-loop-workflow.test.ts
+++ b/tests/boilabin-terminal-loop-workflow.test.ts
@@ -9,6 +9,7 @@ import {
   TERMINAL_LOOP_ACTIVATION_PHRASE,
   createTerminalLoopState,
   extractLatestCommit,
+  extractTitle,
   findRecommendedBroadStaging,
   findSuspiciousSecrets,
   formatTerminalLoopState,
@@ -254,6 +255,11 @@ test('terminal-loop latest commit extraction ignores invalid commit references',
   assert.equal(extractLatestCommit('Commit hash: `zzzzzzz not hex`'), null);
 });

+test('terminal-loop title extraction reads the first markdown H1 only', () => {
+  assert.equal(extractTitle(['Intro', '', '# Step 999 Report Title', '', '## Scope'].join('\n')), 'Step 999 Report Title');
+  assert.equal(extractTitle('## Scope\nNo top-level heading'), null);
+});
+
 test('terminal-loop safe file reader rejects all private env filename variants', () => {
   for (const relativePath of ['.env', '.env.local', '.env.production', '.ENV.PRODUCTION', 'config/.env.staging', 'config\\.env.staging']) {
     assert.throws(
