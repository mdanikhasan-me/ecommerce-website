/**
 * Custom React Hooks
 *
 * Reusable client-side hooks:
 *
 *   useDebounce.ts     → Debounced value for search inputs
 *   useMediaQuery.ts   → Responsive breakpoint detection
 *   useClickOutside.ts → Detect clicks outside a ref
 *   useInfiniteScroll.ts → Infinite pagination
 *
 * Example:
 *   import { useDebounce } from '@/frontend/hooks'
 */

// Hooks will be added here as the app grows
export {}
